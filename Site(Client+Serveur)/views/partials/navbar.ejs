<!-- Référence : https://www.youtube.com/watch?v=kmmxxfyufLE -->
<nav class="navbar navbar-expand-lg fixed-top navbar-light p-md-3 navbar-transparent">
    <div class="container">
        <a class="navbar-brand" href="/pages/index">
            <img src="../images/Concessionnaire_Rubious-removebg-preview.png" style="width: 100px">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="d-flex collapse navbar-collapse" id="navbarNav">
            <div class="mx-auto"></div>
            <ul class="navbar-nav">
                <li class="nav-item m-2">
                    <div class="border border-light rounded-pill justify-content-center bg-light shadow-sm"
                        style="width: 7rem">
                        <a href="/pages/contact" class="nav-link text-center">Contact</a>
                    </div>
                </li>
                <li class="nav-item m-2">
                    <div class="border border-light rounded-pill justify-content-center bg-light shadow-sm"
                        style="width: 7rem">
                        <a href="/pages/catalogue" class="nav-link text-center">Catalogue</a>
                    </div>
                </li>
                <li class="nav-item m-2">
                    <div class="border border-light rounded-pill justify-content-center bg-light shadow-sm"
                        style="width: 7rem">
                        <a href="/pages/connexion" class="nav-link text-center">Connexion</a>
                    </div>
                </li>
                <!--Code pour voir la commande: Temporairement dans le navbar-->
                <li class="nav-item m-2">
                    <div class="border border-light rounded-pill justify-content-center bg-light shadow-sm"
                        style="width: 7rem">
                        <a href="/pages/commande" class="nav-link text-center">Commande</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
    <div class="nav-item ml-auto">

        <div class="dropdown-center">
            <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown"
                data-bs-display="static" aria-expanded="false">
                <i class="bi bi-person-circle"></i>
            </button>
            <ul class="dropdown-menu">
                <li>
                    <div class="dropdown-item bg-light rounded-5" id="userFullName" style="text-align: center;">
                    </div>
                </li>
                <li>
                    <button id="dropdown-item deconnexionBtn" class="btn btn-primary w-100 h-100 rounded-pill"
                        style="font-size: small; text-align: center;" disabled>
                        Déconnexion
                    </button>
                </li>
            </ul>
        </div>

        <div class="justify-content-center col" style="width: 7rem">


        </div>

    </div>
</nav>

<script>
    var nav = document.querySelector('nav');

    window.addEventListener('scroll', function () {
        if (window.pageYOffset > 100) {
            nav.classList.add('bg-body-secondary', 'shadow');
        } else {
            nav.classList.remove('bg-body-secondary', 'shadow');
        }
    });
    function gestionDeconnexion() {
        const userFullName = sessionStorage.getItem('userFullName');
        const btnDeconnexion = document.getElementById('deconnexionBtn');

        if (userFullName) {
            document.getElementById('userFullName').innerText = userFullName;
            btnDeconnexion.disabled = false; // Activer le bouton de déconnexion
        } else {
            document.getElementById('userFullName').innerText = ''; // Effacer le nom d'utilisateur affiché
            btnDeconnexion.disabled = true; // Désactiver le bouton de déconnexion
        }
    }

    document.addEventListener('DOMContentLoaded', function () {
        gestionDeconnexion(); // Appeler la fonction au chargement initial de la page

        document.getElementById('deconnexionBtn').addEventListener('click', function () {
            // Supprimer les informations de session
            sessionStorage.removeItem('userFullName');
            // Rediriger vers la page de connexion ou d'accueil
            window.location.href = '/pages/connexion'; // Modifier l'URL selon votre structure de pages
        });
    });
    document.addEventListener('DOMContentLoaded', function () {
        const urlParams = new URLSearchParams(window.location.search);
        const fullName = urlParams.get('fullName');



        document.getElementById('deconnexionBtn').addEventListener('click', function () {
            // Supprimer les informations de session
            sessionStorage.removeItem('userFullName');
            // Rediriger vers la page de connexion ou d'accueil
            window.location.href = '/pages/connexion'; // Modifier l'URL selon votre structure de pages
        });
    });



</script>